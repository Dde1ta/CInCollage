#include<stdio.h>
#include<string.h>
/*

strlen = lenght of the string => return int (<str>)

strcat = Adds second string to first one => return None (<str>,<str>)

strcpy = change first string to the next one => return None (<str>,<str>)

strcmp = Cheaks 2 string => return -1,0,1 (0 if equal) (<str>,<str>)

*/
int main(){
    // char string_1[50],string_2[50];

    // printf("Enter the string:");
    // gets(string_1);

    // printf("Enter the string:");
    // gets(string_2);

    printf("%d",'A');

    return 0;
}